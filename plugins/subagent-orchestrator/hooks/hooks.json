{
  "description": "Delivers a project's extension content to the orchestrating-subagent-work skill. Both hooks call a self-gating script that stays silent unless that skill is invoked and the project has an extension file at .claude/extensions/subagent-orchestrator/orchestrating-subagent-work.md.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/scripts/inject-extension.sh post-tool-use",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/scripts/inject-extension.sh user-prompt",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
