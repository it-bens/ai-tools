{
  "description": "Delivers a project's extension content to the writing-code, writing-tests, and writing-docs skills. Both hooks call a self-gating script that stays silent unless a software-writer skill is invoked and the project has a matching extension file under .claude/extensions/software-writer/.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/scripts/inject-extension.sh post-tool-use",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/scripts/inject-extension.sh user-prompt",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
