{
  "description": "SessionStart hook to inject file-read MCP tool directives.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'root=\"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}\"; if [ -z \"$root\" ]; then printf \"%s\\n\" \"file-read: plugin root is unavailable\" >&2; exit 1; fi; exec bash \"$root/hooks/scripts/session-start.sh\"'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
